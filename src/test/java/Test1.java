package test.java;

import FMS.FlowerDatabase;
import FMS.MyLogin;
import org.junit.Test;

import java.sql.SQLException;

public class Test1 {
    @Test
    public void test() throws SQLException, ClassNotFoundException, InterruptedException {
        //new MyLogin();
        //new FlowerDatabase().into_excel("flower");
        //Thread.sleep(5000);
        //new MyLogin();

        /*new FlowerDatabase().insertFlowersData(310);
        new FlowerDatabase().insertSuppliesData(310);
        new FlowerDatabase().insertFertilizersData(310);
        new FlowerDatabase().update_ID();*/
    }

}
